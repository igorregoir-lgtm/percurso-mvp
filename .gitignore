data/*.db
data/*.db-journal

# Camada de IA local: pesos de modelo nunca entram no git (ver ai/model-manifest.json);
# o corpus.db do RAG é reconstruível por `node src/rag/ingest.mjs`; doações de interação
# para o dataset LoRA são dado local, nunca versionado.
models/
*.gguf
data/rag/private/
data/rag/*.db*
data/*.jsonl
node_modules/
.DS_Store
*.log
.env
.vercel

# Intermediários do vídeo (o .mp4 final fica versionado)
video/quadros/
video/molduras/
data/*.db-wal
data/*.db-shm
npm-debug.log*
*.tmp
*.bak

# Protótipo de referência Bússola — material comparativo, não é o produto (ver README)
remix-bússola-—-instituto-ebenézer/

# Cache de ferramenta de pesquisa e cópias soltas de trabalho — não são fonte
.firecrawl/
mvp-src-only/
data/passo/
